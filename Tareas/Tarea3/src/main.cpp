#include <iostream>
//#include "Contacto.hpp"
//#include "Funciones.hpp"
#include "HashTable.hpp"
using namespace std;

#define MAX_CONTACTOS 100

int main() {
    /*
    int numContactos = -1;

    Contacto* contactos = (Contacto*)malloc(MAX_CONTACTOS*sizeof(Contacto));


    agregarContacto(contactos,numContactos);

    agregarContacto(contactos,numContactos);

    mostrarContactos(contactos,numContactos);

    free(contactos);
    */
    /*
    HashTable HT;

    if (HT.estaVacio()) {
        cout << "Correct answer. Good job." << endl;
    } else {
        cout << "Oh oh. We need to check out code!" << endl;
    }

    HT.ingresarContacto(905, "Jim");
    HT.ingresarContacto(201, "Tom");
    HT.ingresarContacto(332, "Bob");
    HT.ingresarContacto(124, "Sally");
    HT.ingresarContacto(107, "Sandy");
    HT.ingresarContacto(929, "Bard");
    HT.ingresarContacto(928, "Rob");
    HT.ingresarContacto(928, "Rick");

    HT.imprimirTabla();

    HT.eliminarContacto(332);

    if (HT.estaVacio()) {
        cout << "Oh oh. We need to check out code!" << endl;
    } else {
        cout << "Correct answer! Good job!" << endl;
    }
    */

    return 0;
}